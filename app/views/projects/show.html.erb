Proyecto: <%= @project.name %>
<br>

<%= link_to "Editar", edit_project_path(@project) %> <br>
<%= button_to "Eliminar proyecto", project_path(@project), method: :delete, class: "button is-danger", form: { data: { turbo_confirm: "¿Estas seguro de eliminar este proyecto?" } } %>
