<div class="level">
  <div class="level-left">
    <h2 class="title is-2"><%= @project.name %></h2>
  </div>
  <div class="level-right">
    <%= link_to_modal new_project_ticket_path(@project), class: "button is-primary" do %>
      Nuevo ticket
    <% end %>
  </div>
</div>
<br>
<%= link_to "Editar", edit_user_project_path(current_user, @project) %> <br>
<%= button_to "Eliminar proyecto", user_project_path(current_user, @project), method: :delete, class: "button is-danger", form: { data: { turbo_confirm: "¿Estas seguro de eliminar este proyecto?" } } %>

<div class="is-flex">
  <div class="is-flex is-flex-direction-column p-5">
    <div>To do</div>
    <div>Ticket 1</div>
  </div>
  <div class="is-flex is-flex-direction-column p-5">
    <div>En Progreso</div>
  </div>
  <div class="is-flex is-flex-direction-column p-5">
    <div>Hecho</div>
  </div>
</div>
